#include <vector>

std::vector<int> solution(std::vector<int> nums) {
    if (!nums.empty()) {
        std::vector<int> sortedItems;
        for (char c : nums){
            sortedItems.push_back(c);
            }
    }
}